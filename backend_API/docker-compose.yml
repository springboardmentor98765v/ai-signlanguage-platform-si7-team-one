version: "3.9"

services:
  backend:
    build:
      context: ./backend_API
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./backend_API/.env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000